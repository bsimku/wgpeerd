set(WIREGUARD_SOURCES
    wireguard.c
)

set(WIREGUARD_LIBRARY wireguard)
set(WIREGUARD_INCLUDES ${CMAKE_CURRENT_SOURCE_DIR} PARENT_SCOPE)

add_library(${WIREGUARD_LIBRARY} STATIC ${WIREGUARD_SOURCES})

set(WIREGUARD_LIBRARY ${WIREGUARD_LIBRARY} PARENT_SCOPE)
